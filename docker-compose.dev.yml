
services:
  backend:
    build: .
    container_name: recipe-api
    env_file:
      - .env
    ports:
      - "8080:8080"
  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"

